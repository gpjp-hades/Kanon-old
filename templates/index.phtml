<?php \lib\autoloader::getTemplate('header')?>
  <form id="form" method="POST">
	<input type="hidden" name="state" id="form_state" />
	<?=\lib\csrf::printHTML()?>
  <div class="container">
  <div class="row">
    <div class="col-sm-2">
			<div class="form-group">
				<label for="name">Jméno:</label>
				<input type="text" name="name" class="form-control" id="name" value="<?=$_SESSION['vars']['name']?>" autofocus required />
			</div>
			<div class="form-group">
				<label for="surname">Příjmení:</label>
				<input type="text" name="surname" class="form-control" id="surname" value="<?=$_SESSION['vars']['surname']?>" required />
			</div>
			<div class="form-group">
				<label for="class">Třída:</label>
				<select name="class" class="form-control" id="class">
					<option disabled <?= $_SESSION['vars']['class']=="" ?"selected":""?>>Vyberte třídu</option>
					<option value="a" <?=$_SESSION['vars']['class']=="a"?"selected":""?>>Oktáva A</option>
					<option value="b" <?=$_SESSION['vars']['class']=="b"?"selected":""?>>Oktáva B</option>
					<option value="c" <?=$_SESSION['vars']['class']=="c"?"selected":""?>>4. C</option>
				</select>
			</div>
    </div>
    <div class="col-sm-5">
			<div class="form-group">
				<label for="books">Školní kánon:</label>
				<select name="books" id="books" class="form-control" size="29" ondblclick="send('add')">
		<?php
			foreach($GLOBALS['books'] as $id => $book) {
				echo "<option value='$id'>$book[0] : $book[1]</option>";
			}
		?>
				</select>
			</div>
    </div>
		<div class="col-sm-2">
			<div class="my-buttons">
				<button class="form-control btn btn-default" onclick="send('add')">Přidat <span class="glyphicon glyphicon-menu-right" aria-hidden="true"></span></button>
				<button class="form-control btn btn-default" onclick="send('remove')"><span class="glyphicon glyphicon-menu-left" aria-hidden="true"></span> Odebrat</button>
				<button class="form-control btn btn-danger"  onclick="send('wipe')"><span class="glyphicon glyphicon-ban-circle" aria-hidden="true"></span> Vyprázdnit</button>
			</div>
		</div>
    <div class="col-sm-3">
		<div class="form-group">
		<label for="myBooks">Školní kánon:</label>
		<select name="myBooks" id="myBooks" class="form-control" size="26" ondblclick="send('remove')">
			<?php
				foreach($GLOBALS['myBooks'] as $id => $book) {
					echo "<option value='$id'>$book[0] : $book[1]</option>";
				}
			?>
		</select>
		<button class="form-control btn btn-success" style="margin-top: 15px" onclick="send('finish')">Dokončit seznam <span class="glyphicon glyphicon-ok" aria-hidden="true"></span></button>
    </div>
  </div>
</div>
</form>
</body>
</html>
